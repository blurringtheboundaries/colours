/**
 * Convert gain to dB (via Tone)
 * @param {number} num - Value range 0-1
 * @returns {string} - Value in decibels (0 = '-Infinity')
 */

var v = function(gain){
    return Tone.gainToDb(gain)
}

// module.exports = v;

export { v }