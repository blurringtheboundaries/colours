const map = require('./map');

test('maps from one range to another',()=>{
    expect(map(10,0,10,0,100)).toBe(100);
    expect(map(5,0,10,0,100)).toBe(50);
    
})

test('maps with integer and constrain flags set to true',()=>{
    expect(map(6,0,10,0,100,true,true)).toBe(60);
})
