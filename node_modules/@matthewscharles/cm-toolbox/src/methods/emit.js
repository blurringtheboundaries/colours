/**
 * Dispatches a custom event to the document
 * @param {string} name 
 * @param {object} detail 
 */

export const emit = function(name, detail={}){
    document.dispatchEvent(new CustomEvent(name, {
        detail:detail,
        bubbles: true, 
        cancelable: true, 
        composed: false
    }));
}

