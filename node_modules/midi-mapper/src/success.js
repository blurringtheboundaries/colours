const success = function(midiAccess){
    for(let input of midiAccess.inputs.values()){
        input.onmidimessage = this.process.bind(this);
        console.log('MIDI Mapper listening:', input.name, input);
    }
}

export default success;