/**
 * 
 */

const getRanges = function(channel, pitch, velocity){

    // todo: include exclusivity / override toggle (i.e. action/control assignment overrides note)
    
    let methods = Object.entries(this.map[channel].noteRange)
        .filter(([index,method])=>{
            let range = index.split(',').map(x=>parseFloat(x));
            return pitch >= range[0] && pitch <= range[1];
        })
        .map(x=>x[1]);

    return methods;
}

export default getRanges;

