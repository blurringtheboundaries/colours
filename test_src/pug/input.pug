doctype html
html(lang="en")
    head
        title MIDI Input
        //- script(src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.7.2/p5.js")
        //- script(src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.7.2/addons/p5.dom.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/tone/13.8.6/Tone.js")
        link(rel="stylesheet" href="style.css")
        script(src="./main.js" type="module")
        
    body
        nav.status-bar#connection-status-bar(aria-label="Connection status")
            div#server-status.status(role="status")
                | Server:
                strong Disconnected
            div#midi-status.status(role="status")
                | MIDI:
                strong
                    | Disconnected
            //-- dynamic list of MIDI inputs
            ul#midi-devices.device-list
        .testBars 
            .testBar 0
            .testBar 1
            .testBar 2
            .testBar 3
        .bars
            .bar
            .bar 
            .bar 
            .bar 
            .bar
            .bar 
            .bar 
            .bar 
            .bar
            .bar
            .bar
            .bar
        //- button(onclick="Tone.start()") .